region       = "us-east-1"
project_name = "gitops-platform"

#Assigns values to declared variables

# These are the values for the variables declared in variable.tf. They will be used in the provider and modules. The variables themselves are declared without defaults, so they must be provided here or at runtime.